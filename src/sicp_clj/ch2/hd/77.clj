(ns sicp-clj.ch2.hd
    (:use [clojure.contrib.math :only []])
    (:use [clojure.contrib.generic.math-functions :only []]))

; 2-77
; (magnitude z)
; -> complex 태그의 magnitude를 dispatch
; -> 이것을 rectangular tag의 magnitude로 dispatch
; 2번? 아 잘 모르겠다. 계속 헷갈린다. 한글로 처음부터 다시 읽어봐야겠다. 일단 커밋.
